apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: loki
  name: grafana-volume-claim
spec:
  storageClassName: ""
  volumeName: grafana-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
