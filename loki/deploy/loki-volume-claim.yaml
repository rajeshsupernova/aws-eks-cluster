apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: loki
  name: loki-volume-claim
spec:
  storageClassName: ""
  volumeName: loki-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
